import React from 'react';
import { Input, Button, Select, Table, TableBody, TableCell, TableHead, TableRow } from './UI';

const BudgetList = () => {
  return (
    <div>
      <h2>Budgets</h2>
      <Button>Back</Button>
    </div>
  );
};

export default BudgetList;